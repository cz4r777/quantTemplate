[pytest]
# importlib mode lets pytest collect identically-named test files from
# multiple trees without basename collisions. Required because each
# bot version (v1.2, options-v1.2, options-v1.3) has its own tests/
# directory with overlapping filenames (test_phantom_guard.py etc.).
addopts = --import-mode=importlib
testpaths = tests
